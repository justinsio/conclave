services:
  seed:
    build: .
    image: conclave-seed:latest
    read_only: true
    user: seed
    env_file: .env
    networks: [ conclave-internal ]
    restart: unless-stopped
    tmpfs: [ /tmp ]
